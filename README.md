# ONLINE-CALCULATOR
A simple online calculator built with Node.js and Express. Supports query string operations (add, subtract, multiply, divide) and returns results in plain text or JSON. Includes sample data and API documentation.
